// Minimal JavaScript for homepage
import "@hotwired/turbo-rails"
import "../assets/stylesheets/application.tailwind.css";

document.addEventListener("turbo:load", () => {
  // Turbo load event handler for homepage
});
