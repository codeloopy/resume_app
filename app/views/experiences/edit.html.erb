<div class="max-w-md mx-auto my-4">
  <h1 class="text-2xl font-bold mb-6">New Experience</h1>

  <%= render "form", experience: @experience %>

  <div class="mt-6">
    <%= link_to "Back", edit_resume_path(@resume), class: "text-blue-600 hover:underline" %>
  </div>
</div>
