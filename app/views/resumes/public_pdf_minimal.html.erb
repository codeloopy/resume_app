<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= resume.user.first_name %> <%= resume.user.last_name %> - Resume</title>
    <style>
      @media print { body { print-color-adjust: exact; } }
      body { font-family: "Helvetica Neue", Helvetica, Arial, sans-serif; font-size: 10px; line-height: 1.5; color: #1a1a1a; margin: 0; padding: 40px; background: white; }
      .container { max-width: 700px; margin: 0 auto; }
      header { margin-bottom: 40px; }
      h1 { font-size: 18px; font-weight: 300; letter-spacing: 0.15em; text-transform: uppercase; margin: 0 0 4px 0; color: #1a1a1a; }
      .title { font-size: 11px; color: #666; font-weight: 400; }
      .contact { font-size: 9px; color: #888; margin-top: 8px; }
      h2 { font-size: 9px; font-weight: 600; letter-spacing: 0.2em; text-transform: uppercase; color: #999; margin: 32px 0 12px 0; padding-bottom: 4px; border-bottom: 1px solid #e5e5e5; }
      .experience-item, .education-item, .project-item { margin-bottom: 20px; }
      .experience-item h3, .education-item h3, .project-item h3 { font-size: 10px; font-weight: 600; margin: 0 0 2px 0; }
      .meta { font-size: 9px; color: #888; margin-bottom: 6px; }
      ul { margin: 0; padding: 0 0 0 14px; }
      li { margin-bottom: 3px; font-size: 10px; }
      .skills-list { font-size: 10px; color: #555; }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1><%= user_full_name(resume) %></h1>
        <div class="title"><%= resume.title %></div>
        <div class="contact">
          <% parts = [] %>
          <% parts << resume.user.location if resume.user.location.present? %>
          <% parts << styled_telephone_number(resume.user.phone) if resume.user.phone.present? %>
          <% parts << resume.user.email if resume.user.email.present? %>
          <% parts << "/in/#{resume.user.linked_in_url.split('/').last}" if resume.user.linked_in_url.present? %>
          <% parts << resume.user.github_url.split('//').last if resume.user.github_url.present? %>
          <%= parts.join(" · ") %>
        </div>
      </header>

      <% if resume.summary&.body&.present? %>
        <h2>Summary</h2>
        <div><%= resume.summary.body.to_s.html_safe %></div>
      <% end %>

      <% if resume.skills.any? %>
        <h2><%= skills_title(resume) %></h2>
        <div class="skills-list"><% resume.skills.each do |skill| %><%= skill.name %><%= skill == resume.skills.last ? "" : ", " %><% end %></div>
      <% end %>

      <% if resume.experiences.any? %>
        <h2>Experience</h2>
        <% resume.experiences.each do |exp| %>
          <div class="experience-item">
            <h3><%= exp.title %> — <%= exp.company_name %></h3>
            <div class="meta"><%= exp.location %> · <%= exp.start_date&.strftime("%b %Y") %> – <%= exp.end_date&.strftime("%b %Y") || "Present" %></div>
            <% if exp.responsibilities.any? %><ul><% exp.responsibilities.each do |b| %><li><%= b.content %></li><% end %></ul><% end %>
          </div>
        <% end %>
      <% end %>

      <% if resume.educations.any? %>
        <h2>Education</h2>
        <% resume.educations.each do |edu| %>
          <div class="education-item">
            <h3><%= edu.school %></h3>
            <div class="meta"><%= edu.field_of_study %> · <%= edu.start_date&.year %> – <%= edu.end_date&.year || "Present" %></div>
          </div>
        <% end %>
      <% end %>

      <% if resume.projects.any? %>
        <h2>Projects</h2>
        <% resume.projects.each do |proj| %>
          <div class="project-item">
            <h3><%= proj.title %></h3>
            <% if proj.url.present? %><div class="meta"><%= proj.url %></div><% end %>
            <% if proj.description&.body&.present? %><div><%= proj.description.body.to_s.html_safe %></div><% end %>
          </div>
        <% end %>
      <% end %>
    </div>
  </body>
</html>
