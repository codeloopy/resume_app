<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Resume PDF</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      font-size: 12px;
      line-height: 1.4;
      color: #333;
      margin: 0;
      padding: 0;
    }

    /* Ensure images and external resources load properly */
    img {
      max-width: 100%;
      height: auto;
    }

    /* Fallback styles in case Tailwind doesn't load */
    .fallback-text {
      font-size: 14px;
      margin-bottom: 8px;
    }

    .fallback-heading {
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 12px;
      color: #2d3748;
    }

    /* Ensure proper page breaks */
    .page-break {
      page-break-before: always;
    }

    /* Prevent orphaned headings */
    h1, h2, h3 {
      page-break-after: avoid;
    }

    /* Prevent broken lists */
    li {
      page-break-inside: avoid;
    }
  </style>

  <!-- Load Tailwind CSS with fallback -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" onerror="this.onerror=null; this.href=''; console.log('Tailwind CSS failed to load');">
</head>
<body class="antialiased text-slate-800 leading-relaxed bg-white max-w-3xl mx-auto">
  <%= yield %>
</body>
</html>
