<%# locals: title:, message:, cta: "Upgrade" %>
<div class="mt-6 rounded-[1.75rem] border border-white/10 bg-white/5 p-5 text-slate-100 backdrop-blur">
  <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
    <div>
      <p class="site-kicker">Upgrade path</p>
      <p class="mt-2 text-lg font-semibold text-white"><%= title %></p>
      <p class="mt-1 text-sm text-slate-300"><%= message %></p>
    </div>
    <%= link_to cta, pricing_path, class: "site-btn-primary shrink-0" %>
  </div>
</div>
